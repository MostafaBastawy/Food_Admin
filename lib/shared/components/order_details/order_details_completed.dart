import 'package:flutter/material.dart';

class CompletedOrderDetails extends StatelessWidget {
  const CompletedOrderDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
