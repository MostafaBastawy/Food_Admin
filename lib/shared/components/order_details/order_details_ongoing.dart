import 'package:flutter/material.dart';

class OngoingOrderDetails extends StatelessWidget {
  const OngoingOrderDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
